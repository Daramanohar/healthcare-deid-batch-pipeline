# Copy this file to .env before running locally.
# Docker execution does not require .env - config defaults work out of the box.

PIPELINE_ENV=production
LOG_LEVEL=INFO
INPUT_DICOM_DIR=DICOMs
INPUT_PDF_DIR=PDFs
OUTPUT_DIR=outputs/task1_pipeline
DATABASE_PATH=outputs/task1_pipeline/metadata/pipeline.db
